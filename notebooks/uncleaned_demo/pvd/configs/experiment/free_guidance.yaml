# @package _global_
defaults:
  - override /model: ldm_big_phase2.yaml
  - override /paths: default.yaml

paths:
  save_dir: ${paths.work_dir}/results/free_guidance/${name}/${now:%m%d_%H%M%S}

model:
  classifier_free_guidance: true
  draw_tsne: false
